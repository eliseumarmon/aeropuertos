services:
  php:
    image: php:fpm
    build: ./php
    container_name: aeropuerto_php
    volumes:
      - ./public:/var/www/html
      - ./src:/var/www/src
      - ./php/conf.d/99-xdebug.ini:/usr/local/etc/php/conf.d/99-xdebug.ini
    environment:
      - MYSQL_HOST=mysql # nombre del servicio mysql en docker-compose
      - MYSQL_USER=alumno
      - MYSQL_PASSWORD=alumno
      - MYSQL_DB=aeropuerto

  nginx:
    image: nginx:latest
    container_name: aeropuerto_nginx
    ports:
      - "8080:80"
    volumes:
      - ./public:/var/www/html
      - ./src:/var/www/src
      - ./src/logs:/var/www/src/logs
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php

  mysql:
    image: mysql:8
    container_name: aeropuerto_mysql
    environment:
      MYSQL_ROOT_PASSWORD: administrador
      MYSQL_USER: alumno
      MYSQL_PASSWORD: alumno
      MYSQL_DATABASE: aeropuerto
    volumes:
      - ./mysql/data:/var/lib/mysql
      # - ./mysql/tmp:/tmp
    ports:
      - "3306:3306"
    networks:
      - default

networks:
  default:
    driver: bridge
